package br.gov.am.prodam.folha.routes.service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import br.gov.am.prodam.folha.routes.repository.RoutesApiConfigRepository;

@Service
public class RoutesApiConfigService {
    private final RoutesApiConfigRepository repository;

    @Autowired
    public RoutesApiConfigService(RoutesApiConfigRepository repository) {
        this.repository = repository;
    }

    // methods to handle your business logic
}
